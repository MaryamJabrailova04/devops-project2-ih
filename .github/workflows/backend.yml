name: Backend CI-CD

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/backend.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Maven
        uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('backend/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run Tests
        run: mvn clean test

      - name: Package Backend
        run: mvn clean package -DskipTests

      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v6
        with:
          name: backend-jar
          path: backend/target/*.jar

  sonar-scan:
    needs: build-test
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Run SonarQube Scan
        working-directory: backend
        run: |
          mvn sonar:sonar \
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }}

  deploy:
    needs: [build-test, sonar-scan]
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download Backend Artifact
        uses: actions/download-artifact@v5
        with:
          name: backend-jar
          path: /home/azureuser/artifacts/backend

      - name: Rename JAR
        run: |
          cd /home/azureuser/artifacts/backend
          JAR_FILE=$(ls *.jar | head -n 1)
          cp "$JAR_FILE" backend.jar

      - name: Deploy Backend with Ansible
        run: |
          cd /home/azureuser/config/ansible
          ansible-playbook playbooks/backend.yml

      - name: Validate Backend Direct
        run: curl http://10.20.3.4:8080/health

      - name: Validate Through App Gateway
        run: curl http://20.205.212.128/api/orders/history
